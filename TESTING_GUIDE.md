# 功能测试指南

## 当前应用状态

✅ 应用已成功启动  
✅ 搜索功能错误已修复  
✅ 数据更新错误处理已完善  
✅ 使用模拟数据进行测试

## 测试清单

### 1. 基础功能测试

#### 1.1 搜索功能
- [ ] 输入中文搜索（如："jenkins"）
- [ ] 输入拼音全拼搜索（如："jira"）
- [ ] 输入拼音首字母搜索（如："jk" 应该能找到 "Jenkins"）
- [ ] 使用上下箭头键选择搜索结果
- [ ] 按 Enter 键打开选中的链接
- [ ] 按 ESC 键关闭搜索下拉框

#### 1.2 视图切换
- [ ] 点击"卡片"按钮，查看卡片视图
- [ ] 点击"列表"按钮，查看列表视图
- [ ] 验证两种视图下数据显示正确

#### 1.3 链接操作
- [ ] 点击导航卡片，验证链接是否在新标签页打开
- [ ] 点击复制按钮，验证链接是否复制到剪贴板
- [ ] 查看是否有成功提示

### 2. 数据更新功能

#### 2.1 更新数据（会失败，这是预期的）
- [ ] 点击"更新数据"按钮
- [ ] 等待加载（约10秒超时）
- [ ] 查看是否显示错误提示："更新失败: ..."
- [ ] 验证应用仍然显示模拟数据

**说明**：当前默认数据源地址（`http://192.0.2.142:1234/`）不可访问，所以会失败。这是正常的，实际使用时需要配置正确的数据源地址。

### 3. 系统托盘功能

#### 3.1 最小化到托盘
- [ ] 点击窗口的最小化按钮
- [ ] 检查系统托盘是否显示应用图标
- [ ] 右键点击托盘图标
- [ ] 查看菜单是否显示"显示主窗口"和"退出"

#### 3.2 托盘菜单操作
- [ ] 点击"显示主窗口"，验证窗口是否重新显示
- [ ] 点击"退出"，验证应用是否完全关闭

### 4. 设置页面功能

#### 4.1 访问设置页面
目前主页面没有设置入口，需要手动实现或通过路由访问。

**待完成功能**：
- [ ] 在主页面添加设置按钮
- [ ] 实现路由跳转到设置页面

#### 4.2 自动启动设置
- [ ] 打开设置页面
- [ ] 切换"开机自动启动"开关
- [ ] 验证是否显示成功提示
- [ ] 重启电脑验证自动启动是否生效

### 5. 性能测试

#### 5.1 响应速度
- [ ] 搜索响应是否流畅（无卡顿）
- [ ] 视图切换是否快速
- [ ] 滚动是否平滑

#### 5.2 内存占用
- [ ] 打开任务管理器
- [ ] 查看应用内存占用（正常应该在 50-100MB 左右）

## 已知问题

### 1. 数据更新一直加载
**原因**：默认数据源地址不可访问  
**状态**：已修复 - 添加了错误处理和超时提示  
**影响**：现在会在10秒后显示错误提示，不会一直加载

### 2. 搜索功能报错
**原因**：拼音转换函数返回值类型问题  
**状态**：已修复 - 强制转换为字符串  
**影响**：搜索功能现在应该可以正常使用

### 3. 设置页面无入口
**原因**：主页面没有添加设置按钮  
**状态**：待实现  
**解决方案**：需要在 Home.vue 添加设置按钮和路由

## 模拟数据说明

当前应用使用以下模拟数据进行测试：

```javascript
[
  { name: '集采云平台', url: 'http://192.0.2.100:8080', group: '业务系统' },
  { name: '供应商门户', url: 'http://192.0.2.101:8080', group: '业务系统' },
  { name: '数据分析平台', url: 'http://192.0.2.102:3000', group: '数据服务' },
  // ... 更多数据
]
```

实际使用时，这些数据应该从真实的数据源（如内部网页）抓取。

## 实际部署配置

要在实际环境中使用，需要：

1. **配置数据源地址**
   - 修改 `src/stores/nav.ts` 中的 `sourceUrl`
   - 确保数据源页面包含 `const data = [...]` 格式的数据

2. **实现数据抓取**
   - 后端已实现 HTML 解析逻辑
   - 需要确保数据源格式符合解析规则

3. **测试真实数据**
   - 点击"更新数据"按钮
   - 验证是否能成功抓取并显示

## 下一步开发建议

1. **添加设置入口**
   - 在主页面添加设置图标按钮
   - 实现路由跳转

2. **完善数据源配置**
   - 在设置页面添加数据源地址配置
   - 支持保存和加载配置

3. **增强错误提示**
   - 更详细的错误信息
   - 提供解决建议

4. **添加数据导入导出**
   - 支持手动导入导航数据
   - 支持导出当前数据

5. **实现版本查询功能**
   - 显示各个系统的版本信息
   - 支持版本对比

---

**测试时间**：2026-09-07  
**应用版本**：v1.0.0  
**测试环境**：Windows 11
